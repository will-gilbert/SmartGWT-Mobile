package com.smartgwt.mobile.client.widgets;

public interface Resettable {

    /**
     * Resets this component to its initial state as if it were re-created.
     * <p>
     * Note that it is important to call the super implementation if one exists.
     */
    public void reset();
}
